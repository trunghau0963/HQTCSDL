import * as elements from "typed-html";
import AdminPage from "../admin";

const Schedule = () => {
  return (
    <AdminPage>
      <div>Schedule</div>
    </AdminPage>
  );
};

export default Schedule;
