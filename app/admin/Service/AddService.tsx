import * as elements from "typed-html";
import AdminPage from "../admin";
import AddService from "../../../components/Service/add_service";

const AddServicePage = () => {
  return (
      <AddService/>
  );
};

export default AddServicePage;
